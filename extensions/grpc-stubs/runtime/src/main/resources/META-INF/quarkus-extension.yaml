---
name: gRPC Stubs
artifact: ${project.groupId}:${project.artifactId}:${project.version}
metadata:
  keywords:
    - "grpc"
    - "stubs"
  categories:
    - "serialization"
description: "Pre-compiled gRPC stubs for Pipeline services"